/**
 * Map Configuration Constants
 * Contains default settings for map display
 */

// UK center coordinates (approximately centered on the UK)
// Shows entire UK from Scotland to England
export const UK_CENTER = {
  latitude: 54.5,
  longitude: -2.0,
  latitudeDelta: 20.0,
  longitudeDelta: 20.0,
};

export const MAP_STYLE = {
  flex: 1,
};

// Polygon styling
export const POLYGON_STYLE = {
  strokeColor: '#0066CC',
  strokeWidth: 3,
  fillColor: 'rgba(0, 102, 204, 0.3)',
};

// Marker styling for polygon points
export const POLYGON_MARKER_STYLE = {
  fillColor: '#0066CC',
  strokeColor: '#FFFFFF',
  strokeWidth: 2,
};

